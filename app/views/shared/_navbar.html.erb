<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="<%= root_path %>">Home</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
        <li>
          <%= link_to 'Portfolio', portfolios_path, class: 'nav-link' %>
        </li>
        <li>
          <div class="dropdown">
            <a href="#" class="nav-link dropdown-toggle" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= current_user.name %>
              <% if can? :update, User %>
                <span class="font-italic">(moderator)</span>
              <% end %>
            </a>
            <div class="dropdown-menu dropdown-menu-right text-center bg-dark" aria-labelledby="dropdownMenuLink">
              <% if can? :update, User %>
                <%= link_to 'User list', users_path, class: 'nav-link' %>
              <% end %>
              <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'nav-link' %>
            </div>
          </div>
        </li>
      <% else %>
        <li><%= link_to 'Log in', new_user_session_path, class: 'nav-link ml-3' %></li>
        <li><%= link_to 'Sign up', new_user_registration_path, class: 'nav-link' %></li>
      <% end %>
    </ul>
  </div>
</nav>