<div class="d-flex justify-content-between align-items-center">
  <h3>
    <%= @user.name %> (<%= @user.role %><%= " banned for #{@user.ban} days" unless @user.ban.zero? %>)
  </h3>
    <%= link_to 'Ban User', user_path(@user), class: 'btn btn-danger btn-sm', data: { remote: true, method: :update} %>
</div>
<h5><%= @user.email %></h5>

<%= render @user.comments %>
